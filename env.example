# Environment Variables Template

Copy this file to `.env.local` and fill in your actual values.

```bash
# Google Analytics 4 Measurement ID
# Get this from: https://analytics.google.com/
# Format: G-XXXXXXXXXX
NEXT_PUBLIC_GA_MEASUREMENT_ID=your_ga_measurement_id_here
```

## How to use:

1. Copy this file to `.env.local`:
   ```bash
   cp env.example .env.local
   ```

2. Edit `.env.local` and replace `your_ga_measurement_id_here` with your actual Google Analytics 4 Measurement ID

3. Build your static site:
   ```bash
   npm run build
   ```

4. The environment variables will be baked into the static HTML at build time
